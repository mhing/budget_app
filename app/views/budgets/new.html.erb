<% provide(:title, "Create Budget") %>

<% if signed_in? %>
  <div class="center budget-creator">
    <h3>Create A New Budget</h3>
    <div class="row">
      <div class="span4"></div>
      <div class="span4">
        <%= render 'shared/budget_form' %>
      </div>
    </div>
  </div>
<% else %>
  <%= redirect_to root_path %>
<% end %>