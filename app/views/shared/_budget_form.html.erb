<%= form_for(@budget) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<%= f.label :amount %>
	<%= f.text_field :amount %>
	<%= f.submit "Save", class: "btn btn-primary" %>
<% end %>