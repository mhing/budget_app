<% provide(:title, "Edit Income") %>

<% if signed_in? %>
  <div class="center budget-creator">
    <h3>Edit Income</h3>
    <div class="row">
      <div class="span4"></div>
      <div class="span4">
        <%= render 'shared/income_form' %>
      </div>
    </div>
  </div>
<% else %>
  <%= redirect_to root_path %>
<% end %>